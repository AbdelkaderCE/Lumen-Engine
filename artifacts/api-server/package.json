{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run python -m uvicorn app.main:app --host 0.0.0.0 --port 8080",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "build": "echo 'No build step for FastAPI'",
    "typecheck": "echo 'No typecheck for Python'"
  }
}
